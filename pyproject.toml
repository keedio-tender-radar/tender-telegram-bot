[project]
name = "tender-telegram-bot"
version = "0.1.0"
description = "Bot de Telegram (radar diario) de Keedio Tender Radar"
requires-python = ">=3.12"
dependencies = [
  "pydantic>=2.6",
  "pydantic-settings>=2.5",
  "httpx>=0.27",
  "python-telegram-bot>=21",
  "tender-contracts",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
